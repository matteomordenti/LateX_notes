\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb} % Math packages
\usepackage{geometry}
\usepackage{hyperref} % Clickable links
\usepackage{graphicx} % Images

\geometry{a4paper, margin=1in}

\title{Linguaggi di programmazione appunti}
\author{Matteo}
\date{\today}

\begin{document}

\maketitle

\section{secondo semestre}
\section{ambiente e scope}
ling dichiarativi vs imperativi.

ambiente diviso in3 tipi:
\begin{itemize}
\item ambiente locale: creato all'inizio di un blocco
\item ambiente globale: aambiente relativo all'associazione etichetta:valore comuune  a tutti i blocchi
\item ambiente non locale: associazione ereditata da specifici blocchi(es: un blocco interno ad un altro blocco)
\end{itemize}


ci sono diverse operazioni che si possono fare sull'ambiente, 2 molto importanti sono: \textbf{disattivazione dell'ambiente} e  \textbf{riattivazione dell'ambiente}

la disattivazione annulla il legame nome-oggetto, la riattivazione (?)lo fa uscire dal blocco

scope statico vs dinamico:

statico le associazioni sono note al tempo della compilazione, piu complesso da implementare ma piu efficente

dinamico: informazione derivata dalll'esecuzione, programma piu leggibile e semplice da implementare ma meno efficente.


\section{gestione della memoria}

studiamo gestiione di memoria a linguagi per il fatto che bisogna allocare memoriia durante l'esecuzione dei programmi.

\textbf{allocazione statica della memoria}: un oggetto ha indirizzo assoluto che mantiene per tutta la durata del programma. allocazione statica non ammettte ricorsione.

\textbf{allocazione dinamica}: a run time possono esistere piu istanze della stessa variabile locale.

ogni istanza ha \underline{record di attivazione}

poi esiste la pila per gestire i record di attivazione

slide 12: puntatore di catena dinamica: serve a collegare con il prossimo recoord

indirizzo del risultato, dove scrivere il risultato.

a volte puo servire gestione dinamica della memoria, ma quella con la pila non basta.

2 problemi principali nella gestione dell'heap:

velocità di accesso in tempi non abbastanza veloci

gestione delle varie dimensioni dei blocchi.\\

esiste sia la frammentazione interna che esterna.


nel record di attivazione c'è anche il link statico, che in ogni momento punta al record di attivazione del blocco che contiene il testo del blocco di esecuzione.

quando ci sono procedure annidate, il link statico e invocato attraverso il puntatore di catena statico del chiamante.

slide 36 5.memoria 

display alternativa alla catena statica

CRT mantiene le posizioni delle variabili, alfa1, beta 1 eccetera rappresenntano le posizione della varibile: beta2 vuol dire la seconda variabile della seconda funzione, beta 3 sarebbe la terza variabile della seconda funzione.

\section{gestione del controllo}

\textbf{comando}: entita sintattica la cui valutazione non restituisce necessariamente un valore.


principi della programmazione strutturata nata negli anni 70 per evitare i problemi di spagheti code creati dall'uso improprio dei goto

perche una definizione induttiva sia fatta bene il termine n deve essere definito in base di un termine piu piccolo di n.


fattoriale èuo essere specialmente utile per instruction records.

\section{sottoprogrammi}

 passaggio di parametri per valore unico tipo di pass di parametri in c
 devi fare un assegnamento di valore
 il vantaggio e che sai che il tuo parametro attuale non verra modificato
 ma e molto costoso per grandi quantita di dati


nel passaggio per riferimento, hai un parametro x, crei un'altro parametro y e copi la locazione di memoria di x al suo valore. poi elimini il primo riferimento.










\end{document}